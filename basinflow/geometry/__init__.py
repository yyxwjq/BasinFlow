"""Geometry utilities: minimum-image coordinate math and graph construction.

Import submodules explicitly (``basinflow.geometry.graph`` etc.).
This package deliberately re-exports nothing: the graph builders depend on
``basinflow.data``, so an eager re-export would close an import cycle.
"""
